<div class="container-fluid">

  <div class="masthead">
      <h3 class="text-muted">Your Week at GA</h3>   
  </div>

     <div class="jumbotron home-style">
        <% @weeks.each do |week| %>
            <%= link_to(week.name, week_path(week), {class:"btn btn-default", id:"week-name-button"})%>

        <% end %>


        <br>
          <%= link_to("New Week", new_week_path, {class:"btn btn-success"})%>
    </div> 
</div>


</div>