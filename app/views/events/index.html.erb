<%= render "shared/navbar" %>
<br>
<br>
<br>
<div class="container-fluid">

     <div>
       <center><%= month_calendar table: {class: "table table-bordered"}, tr: {class: "calendar-row"}, td: {class: "day"}, events: @events do |date, events| %></center>

          <div>
          <strong><%= date.day %></strong>
          </div>

          <br>
            <div>
                <% events.each do |event| %>
                    <%= event.name %>   |    <%= event.time %>
                <% end %>
            <div>

      <% end %> <!-- end of month_calendar -->
    </div> 


</div>



